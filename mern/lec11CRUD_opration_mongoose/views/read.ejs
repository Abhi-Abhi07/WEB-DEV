<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-300 min-h-screen p-10">
        <a class="text-blue-600 font-medium" href="/">Go back to home</a>
        <h1 class="pb-4 text-3xl">All User</h1>
        <div class="users flex gap-3 flex-wrap">
        <% users.forEach((user)=>{ %>
            <div class="users bg-zinc-600 p-4 rounded-md w-72">
                <div class="images bg-red-500 h-72 p-2 overflow-hidden rounded-md ">
                    <img class="w-full h-full object-cover" src="<%= user.image %>" alt="<%= user.name%>" >
                </div>
                <h3 class="name text-zinc-400 text-2xl"><%= user.name%></h3>
                <h5 class="email text-zinc-400 text-xl my-2"><%= user.email%></h5>
                <div class="flex flex-wrap justify-between">
                    <a href="/edit/<%= user._id%>">Edit User</a>
                    <a href="/delete/<%= user._id %>">Delete User</a>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>